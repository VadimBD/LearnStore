@model LearnStore.Admin.Models.SellerViewModel

@{
    ViewData["Title"] = AdminLocalizer["CreateSellerTitle"];
}

<nav aria-label="breadcrumb" class="mb-3">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">@SharedLocalizer["HomeBtn"]</a></li>
        <li class="breadcrumb-item"><a asp-action="Index">@AdminLocalizer["SellersLb"]</a></li>
        <li class="breadcrumb-item active" aria-current="page">@ViewData["Title"]</li>
    </ol>
</nav>

<div class="row">
    <div class="col-12 col-md-6">
        <div class="card shadow-sm">
            <div class="card-body">
                <h2 class="h5 mb-3">@ViewData["Title"]</h2>
                <form asp-action="Create" method="post">
                    @Html.AntiForgeryToken()
                    @await Html.PartialAsync("_SellerForm", Model)

                    <div class="mt-3 d-flex gap-2">
                        <button type="submit" class="btn btn-primary">@AdminLocalizer["CreateBtn"]</button>
                        <a asp-action="Index" class="btn btn-outline-secondary">@AdminLocalizer["CancelBtn"]</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}
